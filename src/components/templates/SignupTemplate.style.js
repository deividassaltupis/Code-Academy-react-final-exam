import styled from "styled-components";

export const StyledSignupTemplate = styled.div`
    width: 1000px;
    height: 700px;
    background-color: var(--c-red);
    border-radius: 20px;
    border-radius: 20px;
    .header {
        width: 500px;
        padding: 30px;
        box-sizing: border-box;
        .logo {
            top: 20px;
        }
        .header-img {
            width: 100%;
        }
        p {
            width: 100%;
            bottom: 30px;
            left: 0;
            text-align: center;
            color: var(--c-white);
        }
    }
`;
